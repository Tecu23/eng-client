import Game from "@/components/game/Game";

const App = () => {
    return (
        <main className="flex min-h-screen items-center justify-center">
            <Game />
        </main>
    );
};

export default App;
